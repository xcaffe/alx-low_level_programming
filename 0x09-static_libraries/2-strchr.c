#include <stdio.h>
#include "main.h"
/**
 * _strchr - returns pointer to the first occurrence
 * @s: string
 * @c: character
 * Return: Null if not found
 */
char *_strchr(char *s, char c)
{
	return strchr(s, c);
}
