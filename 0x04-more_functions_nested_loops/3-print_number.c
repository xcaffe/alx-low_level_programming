#include "main.h"

/**
 * print_numbers - function checks for a digit.
 * @c: input variable
 * Return: 1 or 0 should be the value.
 */
void print_numbers(void)
{
        if (c >= 0 ; && c <= 9)
        return (1);
        else
        return (0);
}
