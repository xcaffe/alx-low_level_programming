#include "main.h"

/**
 * mul - function multiplies two integer.
 * @a: input first number
 * @b: input second input
 * Return: value 0 should be a multiply b.
 */
int mul(int a, int b)
{
	return (a * b);
}
